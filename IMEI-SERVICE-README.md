# 📱 IMEI Service - دليل الاستخدام

## ✅ تم تطوير صفحة IMEI بنجاح!

تم تحديث صفحة IMEI لتعمل مع API حقيقي (Dhru Fusion API) بدلاً من البيانات الوهمية.

---

## 🔥 المميزات الجديدة

### 1. **فحص IMEI مجاني**
- فحص صحة رقم IMEI باستخدام Luhn Algorithm
- الاتصال بـ API الحقيقي للحصول على معلومات الجهاز
- إذا لم يكن API متاح، يتم عرض فحص أساسي للتحقق من صحة IMEI

### 2. **طلب خدمات IMEI**
- فك قفل الشبكة
- تقرير IMEI متقدم
- إزالة iCloud Lock
- فحص القائمة السوداء

### 3. **تكامل كامل مع Backend**
الآن جميع الطلبات تُرسل إلى:
- `/api/imei/check` - لفحص IMEI
- `/api/imei/order` - لطلب الخدمات
- `/api/imei/services` - للحصول على قائمة الخدمات المتاحة
- `/api/imei/order/:orderId` - لمتابعة حالة الطلب

---

## 🛠️ الملفات المُحدثة

### 1. **ملفات جديدة:**
- `/routes/imei.js` - API endpoints لخدمات IMEI

### 2. **ملفات محدثة:**
- `/js/imei.js` - يتصل الآن بـ API الحقيقي بدلاً من البيانات الوهمية
- `/js/services.js` - إضافة معالجة الطلبات عبر API
- `/server.js` - إضافة route الـ IMEI

---

## 📊 كيف يعمل النظام؟

### فحص IMEI:
```javascript
POST /api/imei/check
Body: { "imei": "123456789012345" }

Response:
{
  "success": true,
  "data": {
    "imei": "123456789012345",
    "valid": true,
    "brand": "Apple",
    "model": "iPhone 13 Pro",
    "status": "نظيف",
    "network": "غير مقفل",
    // ... المزيد من المعلومات
  }
}
```

### طلب خدمة:
```javascript
POST /api/imei/order
Body: {
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "phone": "+212612345678",
  "imei": "123456789012345",
  "serviceType": "unlock",
  "deviceBrand": "apple",
  "model": "iPhone 13 Pro"
}

Response:
{
  "success": true,
  "message": "تم إرسال طلبك بنجاح!",
  "orderId": "abc123",
  "apiOrderId": "xyz789",
  "estimatedTime": "24-48 ساعة"
}
```

---

## 🔌 تكامل Dhru Fusion API

النظام يتصل بـ:
- **URL:** `https://api.ifreeicloud.co.uk`
- **Username:** `elatribrw`
- **API Key:** محفوظ في `config/dhru-api.js`

### الخدمات المتاحة:
1. ✅ فحص IMEI
2. ✅ فك قفل الشبكة
3. ✅ التقارير المتقدمة
4. ✅ فحص الضمان
5. ✅ فحص القائمة السوداء
6. ✅ معلومات iCloud

---

## 🚀 كيفية الاستخدام

### 1. تشغيل السيرفر:
```bash
npm start
```

### 2. فتح الصفحة:
```
http://localhost:3000/pages/imei.html
```

### 3. فحص IMEI:
- أدخل رقم IMEI (15 رقم)
- اضغط "فحص الآن"
- ستظهر النتائج من API

### 4. طلب خدمة:
- اختر الخدمة (فك قفل، تقرير، إلخ)
- املأ النموذج
- سيتم إرسال الطلب للـ API
- ستحصل على رقم طلب للمتابعة

---

## 📝 معالجة الأخطاء

### إذا فشل API:
- النظام يحفظ الطلب في قاعدة البيانات
- يتم وضع حالة "manual_review" للمراجعة اليدوية
- المستخدم يحصل على رسالة تأكيد بأن الطلب مستلم

### التحقق من صحة IMEI:
- يتم التحقق باستخدام Luhn Algorithm
- إذا كان IMEI غير صحيح، يتم رفض الطلب فوراً

---

## 🎯 الخطوات التالية المقترحة

1. **إضافة صفحة متابعة الطلبات:**
   - يمكن للعميل إدخال رقم الطلب
   - عرض حالة الطلب الحالية

2. **لوحة تحكم للعملاء:**
   - تسجيل دخول العملاء
   - عرض جميع طلباتهم السابقة
   - استلام الإشعارات

3. **تحسين معالجة الأخطاء:**
   - إضافة retry logic عند فشل API
   - إشعارات للمسؤول عند الطلبات اليدوية

4. **تحديد Service IDs من Dhru API:**
   - حالياً Service IDs ثابتة (1, 2, 3...)
   - يجب جلبها من `/api/imei/services`

---

## 🔐 الأمان

- ✅ Rate limiting على جميع endpoints
- ✅ Validation للـ IMEI قبل إرسال للـ API
- ✅ Helmet.js للحماية
- ✅ CORS مُفعّل
- ✅ API Keys محمية في .env (يُنصح)

---

## 📞 الدعم

الآن صفحة IMEI جاهزة للعمل بشكل حقيقي! 🎉

للاختبار:
1. افتح: http://localhost:3000/pages/imei.html
2. جرب IMEI صحيح: `490154203237518` (مثال)
3. املأ نموذج الطلب واختبر إرسال طلب حقيقي

---

**ملاحظة:** تأكد من أن MongoDB يعمل لحفظ الطلبات في قاعدة البيانات!
